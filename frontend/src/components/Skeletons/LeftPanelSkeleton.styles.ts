// Skeleton.styles.ts
import { styled } from "@mui/system";

export const SkeletonWrapper = styled("div")({
  display: "flex",
  flexDirection: "column",
  gap: "3px",
  width: "80%",
  margin: "auto",
});
